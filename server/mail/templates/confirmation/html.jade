doctype html
html
    body
        h1 Thank you very much for your gift!

        if giftSet.paymentMethod === PAYMENT_METHODS.PAYPAL
            p Your chosen payment method was by Paypal.
            p Here is the link #{paypalLink}.
        else if giftSet.paymentMethod === PAYMENT_METHODS.BANK_TRANSFER
            p Your chosen payment method was by Bank Transfer.
            p If you do change you mind and want to pay with Paypal, here is the link #{paypalLink}.
        else
            p Your chosen payment method is not supported??

        table(style="text-align: left;")
            thead
                tr
                    th Name
                    th Price (£)
                    th Quantity
                    th Total (£)
            tbody
                each gift in giftSet.gifts
                    tr
                        td #{gift.honeymoonGiftListItem.name}
                        td #{gift.price}
                        td #{gift.quantity}
                        td #{gift.total}

        h3 Total £#{giftSet.total}
